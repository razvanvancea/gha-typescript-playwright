{
  "name": "gha-typescript-playwright",
  "version": "1.0.0",
  "description": "Playwright UI test automation using TypeScript and Jest.",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test --reporter=html"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.6",
    "typescript": "^5.8.2"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
